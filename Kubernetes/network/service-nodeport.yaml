apiVersion: v1
kind: Service
metadata:
  name: multi-container-nodeport
  labels:
    app: multi-container
spec:
  type: NodePort
  selector:
    app: multi-container
  ports:
  - name: nginx
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080
