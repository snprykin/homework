# grafana/provisioning/datasources/datasource.yml
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Используем DNS-имя сервиса из docker-compose
    url: http://prometheus:9090
    isDefault: true
    editable: true
